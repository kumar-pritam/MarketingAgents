"""Optional Google Sheets integration placeholder for Phase 1."""

from __future__ import annotations


def export_to_sheets(_: dict) -> tuple[bool, str]:
    return False, "Sheets export not enabled in Phase 1 build."
